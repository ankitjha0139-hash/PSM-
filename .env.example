# Read at request time by netlify/functions/lib/careerData.mjs (via
# `netlify dev` locally). In production, set these in the Netlify dashboard
# under Site settings -> Environment variables, same as GEMINI_API_KEY.
CAREER_SHEET_URL=https://script.google.com/macros/s/PLACEHOLDER/exec
CAREER_SHEET_API_KEY=PLACEHOLDER
