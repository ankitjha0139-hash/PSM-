[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing fallback — needed once we add React Router in later phases,
# harmless for now since this is a single page.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
